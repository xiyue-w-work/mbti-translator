{
  "name": "mbti-translator",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "test": "node --test tests/*.test.js",
    "demo": "node preview/server.js",
    "build": "node scripts/build-demo.js"
  }
}
